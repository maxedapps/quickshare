export const Limits = {
  idBytes: 16,
  idLength: 22,
  projectSlugMin: 1,
  projectSlugMax: 63,
  pathMaxBytes: 768,
  pathSegmentMaxBytes: 255,
  reservedPathSegment: ".quickshare",
  minFiles: 1,
  maxFiles: 500,
  maxFileBytes: 95_000_000,
  maxTotalBytes: 500_000_000,
  maxManifestBytes: 512 * 1024,
  ttlMinSeconds: 60,
  ttlMaxSeconds: 31_536_000,
  maxEmails: 10,
  passwordMinBytes: 1,
  passwordMaxBytes: 1024,
  emailMaxBytes: 254,
  mediaTypeMaxBytes: 256,
  listDefaultLimit: 50,
  listMaxLimit: 100,
  pendingTtlMs: 24 * 60 * 60 * 1000,
  recoveryWindowMs: 24 * 60 * 60 * 1000,
  cookieMaxMs: 12 * 60 * 60 * 1000,
  pbkdf2Iterations: 600_000,
  pbkdf2SaltBytes: 16,
  pbkdf2DigestBytes: 32,
  mcpMaxFiles: 64,
  mcpMaxFileBytes: 4 * 1024 * 1024,
  mcpMaxTotalBytes: 8 * 1024 * 1024,
  mcpMaxBodyBytes: 12 * 1024 * 1024,
  startRateLimit: 10,
  startRatePeriodSeconds: 60,
  loginRateLimit: 10,
  loginRatePeriodSeconds: 60,
  uploadConcurrency: 4,
  sharedPrincipal: "*",
} as const;

export const DefaultProject = "default";
